import 'package:flutter/material.dart';

class CharacteristicView extends StatelessWidget {
  const CharacteristicView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
